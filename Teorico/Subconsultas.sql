create database Coso;

--Subconsultas, dividen el problema, se ejecuta primero.

--IN, EXISTS, ANY, ALL. Usados para condicionales?, any es propio del mysql tiene la misma funcion que el in y exists.

--En instert con subconsultas lo que devuelve tiene que tener la misma cantidad de campos que los que estan arriba.
